/**
 * Access-hook counter — `bun run hook-server`.
 *
 * Stub until the hook-server slice lands. See DESIGN.md Contracts -> Hook
 * counter HTTP API for the endpoints it will serve on `$PORT_WEB`.
 */
console.error("not implemented");
process.exit(1);
