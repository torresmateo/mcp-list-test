/**
 * Report renderer — `bun run report`.
 *
 * Stub until the report slice lands. See DESIGN.md Contracts -> Report for the
 * summary table it will render from `results/*.json`.
 */
console.error("not implemented");
process.exit(1);
